[build-system]
requires = ["setuptools>=82.0.1", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "moal"
version = "0.1.0"
description = "Cost-aware synthetic active learning pipeline for cheminformatics"
requires-python = ">=3.11"
dependencies = [
    "chemprop>=2.2.3,<3.0",
    "lightning>=2.6.1",
    "torch>=2.11.0",
    "rdkit>=2026.3.1",
    "pandas>=3.0.2",
    "numpy>=2.4.4",
    "scipy>=1.17.1",
    "click>=8.3.2",
    "pyyaml>=6.0.3",
    "rich>=15.0.0",
    "matplotlib>=3.10.8",
    "Pillow>=12.2.0",
    "plotly>=6.7.0",
    "dash>=4.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff",
    "mypy",
    "types-PyYAML",
    "pandas-stubs",
    "scipy-stubs",
    "pre-commit",
]

[project.scripts]
moal = "moal.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["moal*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = [
    # lightning 2.6.1 uses LeafSpec() internally; torch 2.10+ deprecated it.
    # The warning surfaces from lightning's own _pytree.py (the call site).
    # Remove once Lightning ships a fix upstream.
    "ignore:`isinstance\\(treespec, LeafSpec\\)` is deprecated:FutureWarning:lightning\\.pytorch\\.utilities\\._pytree",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "S", "D"]
ignore = ["D205", "D401", "S603", "S607", "N812"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "D100", "D101", "D102", "D103", "D104", "E501", "N806"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.mypy]
python_version = "3.12"
disallow_untyped_defs = true
check_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
warn_unused_configs = true
no_implicit_reexport = true

[[tool.mypy.overrides]]
module = ["chemprop.*", "plotly.*", "dash.*"]
ignore_missing_imports = true
